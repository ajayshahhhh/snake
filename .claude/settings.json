{
  "hooks": {
    "FileChanged": [
      {
        "matcher": "src",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.file_path // empty' | grep -E 'src/.*\\.(cpp|h)$' && ~/.platformio/penv/bin/pio run --target upload --environment metro 2>/dev/null || true",
            "async": true,
            "statusMessage": "Building and uploading sketch..."
          }
        ]
      }
    ]
  }
}
